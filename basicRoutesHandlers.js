app.get('/', function(req, res){
    // fetch from database
    // load pages
    //return JSON
    // full access to request and response

});

/* Benefits
1. Handling requests/routes is simple
2. app.get, app.post, app.put, app.delete etc
3. Access to params, query strings, url parts etc
4. Express has a router so we can store routes in seperate files and export
5.We can parse incoming data with the Body parser

*/